# Loop State — Gu-SEO

Tracker for loop engineering runs. Updated by each loop invocation.
Budget and cadence: see `LOOP.md` + `loop-budget.md`. Safety: see `gate.yaml`.
Binding rules: `loop-constraints.md` (loaded before every run).
Mode: `report-only` (L1, propose only) — flip to `mode: auto-fix` in `loop-constraints.md` after 10 stable runs to enable L2. `loop-pause-all` here stops all loops.

## Active patterns

| Pattern | Cadence | Risk | Readiness | Status |
|---|---|---|---|---|
| issue-triage | 2h–1d (`scripts/loop-run.sh issue-triage`) | low | L1 | active — reads/writes `issue-triage-state.md` |
| daily-triage | 1d (09:00 UTC) | low | L1 | active — merges issue-triage Top 5 into High Priority |
| pr-babysitter | 30m (business hours) | medium | L2 | active |
| ci-sweeper | 15m | high | L2 | active |
| dependency-updater | 1d | medium | L1 | active |
| code-reviewer | on_demand | low | L2 | active |
| test-runner | on_demand | low | L1 | active |
| documentation-sync | 1d | low | L1 | active |

## Budget (from loop-budget.md)

- Daily token cap: 100,000 — monthly: 3,000,000 — per-loop: 10,000
- Kill switch: enabled (daily cap exceeded / 5 consecutive failed loops / provider rate limits). Auto-recovery: disabled, manual intervention required.
- Cost: $50/day hard stop, alert at $40/day. Alert at 80% of daily tokens. Weekly cost reports.

## Gate summary (from gate.yaml)

Denied paths: `schema/init.sql`, `functions/_lib/schema.js`, `functions/_lib/auth.js`, `wrangler.toml`, `.env`, `.env.local`, `package.json`, `node_modules/`.
Human approval required for: database changes, auth changes, production deploys. Auto-merge: enabled in `auto-fix` mode — only `loop/fix-*` branches whose verifier APPROVEd and whose tests + build passed, fast-forward only; denylisted paths and REJECTs still escalate.

## Run log

Format: `[YYYY-MM-DD HH:MM:SS UTC] {pattern-name} - {status} - {details}`
(Full history: `loop-run-log.md`.)

| Timestamp (UTC) | Pattern | Status | Details |
|---|---|---|---|
| 2026-09-16 17:54:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed | Brand admin fix e783b26: theme-color/logo UI added to Brand.jsx, save strips logo_url (fixes logo_url_is_server_assigned), voice_tone/target_audience mapping. Gate ✅. Pushed main, deployed https://51e130b6.gu-seo.pages.dev. Left uncommitted (not mine): App.jsx menu move, wrangler.toml name. |
| 2026-09-16 18:05:00 UTC | manual-feature (ad-hoc, user-requested) | success + deployed (2fb90cc) | Calendar "Tạo bài ngay": per-slot button (scheduled/draft only) in drawer runs blog chain start(calendar_slot_id)→text→image→publish with progress modal. Backend reused as-is (start claims slot, publish marks published). Files: src/admin/pages/Calendar.jsx (+rel=noopener fix). Gate ✅. Pushed main, deployed https://d78cf961.gu-seo.pages.dev. |
| 2026-09-16 18:15:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (25f4ed1) | Move "Bài đăng mạng xã hội" menu item from Phân phối group to Bài viết group (src/admin/App.jsx, pre-existing local change). Gate ✅. Pushed main, deployed https://81f6ab34.gu-seo.pages.dev. Still uncommitted (do-not-commit): wrangler.toml name. |
| 2026-09-17 09:30:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (95b7fe6, 7c65575) | Custom-domain approval 502 root-caused: `CF_API_TOKEN` was valid but missing `Cloudflare Pages: Edit` → 403 code 10000 on `pages/{project}/domains`. Deleted 4 empty secrets that caused the earlier misdiagnoses (`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_KEY`, `CLOUDFLARE_EMAIL`), filled `CF_D1_ID` + `CF_R2_NAME`, reverted the dead global-key fallback in `cloudflare_domains.js`, added an attach-failure log (hostname + CF error, no secrets) in `domains/requests.js`. Approved `blogs.gurouter.com` via wrangler OAuth attach + D1 flip + `audit_log` row, verified live; token later granted `Pages: Edit` and approve returned 200 (`cf_status: initializing`). Temp diagnostic `cf-check.js` added then removed. Gate ✅. |
| 2026-09-17 15:00:00 UTC | manual-feature (ad-hoc, user-requested) | success + deployed (1f70cec) | Brand language selector controls AI output language. `projects.language` is the single source of truth: `brand-dna` GET returns it and PUT validates against a 20-code allowlist (the field lands in the prompt, so free text was a prompt-injection vector); `ai.js` resolves it per `projectId` inside `generateContent`, so blog/programmatic/refresh all obey it, and `preview-sample` was missing `projectId` so previews now match too. UI select in `Brand.jsx`. E2E: `language=en` → 2694-word English preview with 0 Vietnamese diacritics; injection attempt → 400. Gate ✅. Pushed main, deployed https://b4079176.gu-seo.pages.dev. |
| 2026-09-20 11:00:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (cron-worker) | Cron looked dead (newest post 65h). Root-caused with Workers analytics: every 01:00 invocation used **exactly 50 subrequests** — the free-plan per-invocation cap — because the standalone social sweep spends 1 dry_run + 1 call per active project (~30), leaving ~19 for the blog fan-out. `listProjects` orders `created_at DESC`, so the tail of the list — the 10 oldest projects incl. `gulagi` + `gurouter` — was starved for 3 days while the 19 newest published fine. Fix: social is now **one unfiltered tick call** (`runTask(..., { all: true })`); blog/prog/refresh keep per-project fan-out (their chains exceed the edge timeout, social's do not). Blog hour is now 1 + 1 + 29 = 31 ≤ 50. `observability` enabled so a partial fan-out is no longer invisible. Verified: 10:30 tick = 1 subrequest (was 29–30); a forced 11:00 run via a temporary `BLOG_HOUR` secret published all 10 starved projects (`/api/health` posts 178 → 188), secret deleted after. Cron test updated (social = 1 call, blog = per-project). |
| 2026-09-20 14:00:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (68a86c31, 187782d5) | Facebook channel: "Token Facebook không hợp lệ … Tạo Page Access Token mới" on publish, and reconnecting never listed the already-connected Page. Two repo defects, both hiding the real cause: `listManagedPages` **silently dropped** Pages Meta returns without a per-Page token (→ `no_pages` → "Không tìm thấy Page nào"), and `describeGraphError` collapsed Graph **code 190** into one message although it also means "token belongs to a different Page" and subcode **492** ("user has no appropriate role on the Page"); the same failure can arrive as **100/33**. Fixes: keep + flag token-less Pages; precise `no_token` / `select_page` outcomes; on 190 or 100:33 ask `/me` with the token itself to name which Page it belongs to (wired into `verifyFacebookPage` + both publish paths); `configured_page_seen` makes the picker warn when the configured Page is absent from Meta's list, and the empty state distinguishes "list expired" from "Meta returned no Pages". Research: the string is undocumented by Meta but maps to 190 / 100:33 in vendor docs; the account-side remedy (Page role or a different login) stays the operator's. 152 platform checks pass (3 new FB tests + cron routing realigned). |
| 2026-09-21 08:51:09 UTC | manual-fix (ad-hoc, user-requested) | success + merged (b54c309) | Carousel lifecycle after the publish fix: delete.js lists the carousel prefix and removes only `<prefix>-N.png` slides (a bare-prefix list also matched a neighbouring slug's slides — caught by exercising, not skimming); list.js strips the carousel sentinel in the blog_posts join so rows show the article title, and drops the dead `isCarousel` var. Built in a Loop worktree `loop/fix-20260921154352`: npm test 154 checks pass + build:functions compiled, ff-merged to main, worktree/branch removed. Also flipped loop-constraints.md to `mode: auto-fix` and taught scripts/loop-run.sh to ff-merge an APPROVEd fix and log to loop-run-log.md + STATE.md. |
| 2026-09-21 08:58:19 UTC | manual-config (ad-hoc, user-requested) | success | Added `autofix --dry-run` (or `LOOP_DRY_RUN=1`) to scripts/loop-run.sh: implementer + verifier + `npm test` + `build:functions`, prints the diff, logs the run, keeps the `loop/fix-*` branch/worktree for a human — never merges. Testing surfaced two runner blockers: `pause_check` matched the literal `loop-pause-all` so STATE.md's prose mention exited EVERY run; `mktemp /tmp/loop-diff.XXXXXX.patch` (non-trailing X) collided on the second run — now `${TMPDIR:-/tmp}/loop-diff.XXXXXX`. Verified with a stub `opencode`: main unchanged at b54c309. |
| 2026-09-21 09:40:53 UTC | manual-feature (ad-hoc, user-requested) | success + merged + deployed (9efe1e7, 51035e6) | Carousel tách khỏi trang Video thành trang riêng `#carousel` (nhóm Bài viết): chọn bài viết bằng dropdown có tìm kiếm thay vì gõ slug, hướng dẫn 4 bước, nhãn vai trò từng slide (Bìa/Ý/Kêu gọi), xem trước cả bộ 5 slide, tự làm mới khi agent đang render. Files: src/admin/pages/Carousel.jsx (mới), src/admin/App.jsx (menu + route), src/admin/pages/Video.jsx (gỡ carousel, lọc kind !== 'carousel', dọn dead code). Kèm 51035e6: timeout guard cho opencode trong loop-run.sh (LOOP_AGENT_TIMEOUT) + seed issue thật vào issue-triage-state.md. `npm run admin:build` OK. Pushed main, deployed Pages https://e6042544.gu-seo.pages.dev (cron-worker không đổi). Verified live: main.js trỏ Carousel-B--e-QvJ.js và chunk chứa copy mới (HTTP 200). |
| 2026-09-21 10:36:00 UTC | manual-test (user-requested) | success | Smoke test đường render carousel của agent, không cần Chrome/hyperframes/network. Hợp đồng deck: 5 slide 1080×1350 (cover / 3 điểm / CTA), pad đủ 5 slide, accent theo brand + fallback, escape text, HTML tất định, `slideQueries` riêng từng slide. Đường render thật: `renderCarousel` chạy với workspace tạm + `spawn`/`photo`/`deliver` giả — 5 snapshot đúng thứ tự deck (mỗi cái render đúng slide + ảnh của mình), deliver đúng `job_id` + 5 slide đúng thứ tự (khớp `carousel/<slug>-1..5.png`), GuRouter tắt/host ảnh chết vẫn đủ deck, snapshot fail/no-PNG dừng trước deliver, deliver non-2xx hoặc 200-không-thành-công đều fail job. Seam tối thiểu `renderCarousel(job, deps)` + `assetsDir`, batch loop gate theo entry point (gọi trực tiếp vẫn claim như cũ). 4 đột biến đều bị bắt. `npm test` = 4 suite (9 + 157 + 8 + 14). |
| 2026-09-21 10:19:06 UTC | manual-test (loop follow-up, user-requested) | success | Đưa 4 ca của pass verify thành test thường trú thay vì probe ở /tmp. Platform suite thêm mục Q (R2 + Graph giả): carousel trên kênh bật `as_video` upload đủ photo/0 lần `/videos`, `listSocialPosts` trả `post_title` đúng cho job carousel, delete xoá đúng slide của slug mình và giữ slug láng giềng. Script mới `scripts/run-admin-hook-tests.mjs` (+ harness React hook tối thiểu) chạy thật `videoQueue.js` với fetch/timer giả: Video không poll, Carousel poll 20s rồi tự tắt. `npm test` = 3 suite (9 + 157 + 8 check). Xác nhận test bắt được lỗi bằng 4 đột biến — cả 4 fail đúng chỗ; không phải sửa production code. |
| 2026-09-21 13:13:56 UTC | manual-fix (ad-hoc, user-requested) | success | Video không có nhạc nền. Chạy render THẬT lần đầu (npx hyperframes@0.8.56 render, Chrome + ffmpeg có sẵn trên máy) và đo bằng ebur128: bed cũ nằm ở -60.6 LUFS trong khi giọng đọc -29.4 LUFS — tức 31.2 LU dưới giọng (file bed chỉ -33 dBFS peak, và nội dung 400-2000 Hz ở -42 dB), nên video đo ra y như không có nhạc. Nguyên nhân trong `makeBgm`: `amix=3` chia đều 3 input (mất 9.5 dB) + biên độ sine quá thấp + hợp âm ở 220-330 Hz dưới ngưỡng loa điện thoại. Sửa: `amix=inputs=3:normalize=0`, sine 0.30/0.34/0.30, hợp âm lên một quãng tám (440/554/659 và bộ tương ứng), lowpass 2400; thêm log khi KHÔNG có nhạc (trước đây im lặng). Sau sửa, render lại: bed -46.7 LUFS trong mix = 17.3 LU dưới giọng, band 400-2000 Hz -39.4 dB (hơn 21 dB). Thêm 3 check vào `scripts/run-video-agent-tests.mjs` (17 total). Chưa commit. |
| 2026-09-21 10:10:46 UTC | manual-verify (architect pass follow-up, user-requested) | success | Nghiệm thu refactor bằng cách chạy thật. 3 probe, 52 check pass: vòng đời carousel qua đúng handler trên node:sqlite + R2/Graph giả (20), `videoQueue.js` trên React mini-runtime + fetch/timer giả (20) — Video không poll, Carousel poll 20s rồi tự tắt, và mount thật Video.jsx/Carousel.jsx bằng esbuild của repo để chứng minh mỗi trang chỉ hiện đúng kind của mình (12). Lần chạy lộ 2 lỗi thật: `facebook.js` kiểm tra `cfg.asVideo` trước nhánh carousel nên kênh bật `as_video` làm nút "Đăng FB" của carousel hỏng (prefix `carousel/<slug>` bị đưa sang upload video); `listSocialPosts` join bằng `blog_post_id` thô nên job carousel hiện `post_title = null`. Cả hai đã sửa; npm test 154 + build:functions + admin:build đều xanh. |
| 2026-09-21 10:03:59 UTC | manual-refactor (architect pass, user-requested) | success | Kiến trúc cho code carousel/video. BACKEND: `functions/_lib/video_jobs.js` là chủ sở hữu DUY NHẤT của quy ước sentinel ref (carousel:/project:/url:) và hợp đồng slide carousel (carousel/<slug>-N.png, đúng 5 slide) — carousel, carousel-deliver, claim, list, delete, social_queue, facebook, publisher đều gọi lib. FRONTEND: `src/admin/lib/videoQueue.js` (status meta theo kind, loader + polling, publish/delete dùng chung) + `src/admin/components/VideoStatusTag.jsx`; Video.jsx và Carousel.jsx chỉ còn trình bày + luồng tạo riêng. Verify: npm test 154 pass, build:functions OK, admin:build OK (Carousel 8.68→7.41kB, Video 9.91→8.65kB). Real run: probe sqlite create→list→claim→deliver→list→social drain→delete, xoá đúng slide prefix, giữ carousel/alpha-postX-1.png. |
| 2026-09-21 14:24:25 UTC | manual-fix (ad-hoc, user-requested) | success | Master độ to cho mix cuối của video. Render thật đo -29.4 LUFS / -20.3 dBTP (LRA 0.7) — ~15 LU dưới chuẩn mạng xã hội. `masterLoudness()` trong `video-agent/render-video.mjs` đo file đã mix bằng ebur128 rồi nhân TOÀN BỘ mix với MỘT gain tĩnh về -14 LUFS (chặn ở trần -1.5 dBTP), video copy, audio AAC 192k; không đo được thì deliver nguyên bản + log. Dùng gain tĩnh chứ không `loudnorm:linear=true` vì loudnorm chỉ chịu linear khi LRA ≠ 0 và gain không vượt trần TP, còn lại tự chuyển dynamic (gain biến thiên → nâng bed ở khoảng lặng của giọng). Không đụng `data-volume=0.12`/`makeBgm`. Render THẬT mới (hyperframes + Chrome): -29.4 → **-14.1 LUFS**, TP -5.0 dBFS, LRA giữ 0.7, duration/stream như cũ, MỘT gain +15.37 dB (spread 0.19 dB, residual -32 dB) nên tỉ lệ giọng/nhạc không đổi. 4 check mới (21 tổng) đo lại bằng ebur128 độc lập + ca hết headroom + ca không đo được; 4 đột biến đều bị bắt (dynamic, gain 0, gain +12, bỏ trần TP). `npm test` 4 suite (9 + 157 + 8 + 21). |
| 2026-09-21 16:15:19 UTC | manual-fix (job thật trên VPS, user-requested) | success | Lần đầu chạy trọn job video THẬT trên render VPS (`node2-prod`, systemd `pages-seo-video.service`): enqueue qua admin API (limit 1) → timer claim → GuRouter script → edge-tts vi-VN → Chrome render (hyperframes 0.8.56) → master → deliver. RÀNG BUỘC: ffmpeg trên VPS là **4.4.2 (Ubuntu 22.04)**, không phải 8.x như máy dev — nó từ chối `ebur128=peak=true:framelog=quiet` (exit 1, "Error reinitializing filters!"), không in `Summary:` nên `masterLoudness` trả null trên MỌI render thật (log `loudness master skipped`) dù 24 check local xanh (ffmpeg 8.1.2 chấp nhận giá trị đó). Sửa: chỉ dùng `ebur128=peak=true` + log rõ exit code + stderr khi thiếu Summary (bỏ im lặng). VPS còn đang chạy code `7a3aa1e` (không có masterLoudness, bed cũ) nên đã deploy HEAD (backup `.bak-7a3aa1e-20260921-230639`). Đo file đã phát hành `video/giam-chi-phi-bao-bi-cho-doanh-nghiep-2026-1790007139262.mp4`: **-14.6 LUFS / TP -1.5 dBTP / LRA 3.2**; bytes publish = R2 = URL công khai `/image/...` = `master.mp4` trên VPS (sha256 `6203165817…`). Đối chiếu: raw cùng job -17.1; bản code cũ -16.8; bản master-fail-im-lặng -16.6. ĐÍNH CHÍNH: mix THẬT là -16.6…-17.1 LUFS chứ không phải -29 (số -29 từ render tổng hợp local có "giọng" = pink noise) nên master chỉ nâng ~2.5 dB và trần TP chặn trước → -14.6. Không tự đăng Facebook (project không bật `as_video`). `npm test` 4 suite (9 + 162 + 8 + 24). |
| 2026-09-21 14:46:09 UTC | manual-verify (renderOne seam, user-requested) | success | Chứng minh vị trí của bước master: `renderOne` trước đây không có seam nên chỉ `masterLoudness` được test. Thêm `renderOne(job, deps)` với `work`/`spawn`/`deliver` (như renderCarousel), mọi path subprocess đi qua seam, nhánh carousel forward deps, và `makeBgm`/`masterLoudness` nhận out path trong `work`. Test mới (24 check): happy path chỉ giả hyperframes (mọi ffmpeg là thật, render giả đặt đúng mức -29 LUFS) → **bytes gửi deliver y hệt `renders/master.mp4`**, loudness -29 → -14.0 LUFS, TP -9.5, master sau render; hai nhánh hỏng (không có audio / file không đọc được) đều gửi bản thô **kèm log `loudness master skipped`**, không im lặng; job carousel vẫn đi đúng nhánh carousel. Chạy thêm `renderOne` với default `work` (production) ghi đúng vào `video-agent/workspace` rồi dọn sạch. Lộ lỗi harness: `deepEqual` trên Buffer ~100-160KB làm suite bị OOM-kill khi fail (exit 137) → đổi sang `equals()` + so size. 4 đột biến đều bị bắt. `npm test` 4 suite (9 + 162 + 8 + 24). Chưa commit. |
| 2026-09-21 16:36:59 UTC | manual-jev-cost (docs typesafe + đo thật, user-requested) | success | Đọc `/concepts/state`, `/concepts/confidence`, `/models`, cookbook `parallel_questions`. GIÁ: **input token mới tính tiền** ($0.042/Mtok, output free), chỉ một model (`jev-1.13.0`) ⇒ không có tier rẻ hơn, mọi đòn đều là "gửi ít hơn hoặc không gọi". Đo thật: khối 9 câu hỏi tốn ~0.7-1k token **mọi** call (file 613 ký tự = 975 token); diff thật của autofix ~4.5k token (state ~80%). `scripts/jev.js`: gộp N tài liệu vào một call (state `documents[]`, khoá `d<i>_`) — 4 file thật: 4 call → 1 call, 7271 → 7289 token (trung tính), 4.67s → **1.40s (3.3×)**; 12.2× của cookbook không áp cho tài liệu rời (đã ghi trong JEV.md); `--summary` thêm `in=<tokens>` ⇒ chi phí từng gate vào log. Không mua phán quyết khi rỗng: `evaluate` không tài liệu → exit 2 không gọi (shim đếm 0 call), runner bỏ gate khi branch không đổi file (row `nothing to judge`). LỖI THẬT: diff lấy **trước** commit nên rỗng khi implementer tự commit → verifier + denylist + Jev cùng nhìn diff rỗng dù branch có thay đổi (mutation dựng lại được); sửa: commit trước rồi `DIFF_FILE = main...HEAD`, denylist soi cùng diff. Harness 24 check xanh (W1/W2/W3/MUT/T1/E1); dọn 1 row rác `forced: proceed` của stub từ pass trước. `npm test` 4 suite (9 + 162 + 8 + 24), `node --check` + `bash -n` OK. Chưa commit. |
| 2026-09-21 16:39:27 UTC | manual-wire-jev (user-requested) | success | Ghi lại pass nối Jev vào runner (đứng trước row `manual-jev-cost`; cả hai vào một commit). `loop-run.sh`: `jev_call` là chủ duy nhất của "chạy jev, lấy gate" (`JEV_NOTE`/`JEV_RC`, tách stdout/stderr, dùng `awk` để `pipefail` không giết runner); autofix **chỉ merge khi Jev `proceed`**, `review`/`stop` → row `escalated` giữ branch/worktree, `unavailable` (thiếu key/mạng/API lỗi) **không chặn** nhưng ghi lý do; cả hai nhánh triage chấm Top 5 theo rubric `skills/issue-triage/jev-questions.json` **và ghi row** (trước đây không log gì). `jev.js`: `--summary` in một dòng đầy đủ score+confidence trước dòng JSON (parser cũ không vỡ), `!` cho câu dưới ngưỡng, lỗi API in `error=<message>` thay vì JSON thô. Docs: sửa câu SAI trong JEV.md ("exit code là quyết định" chỉ đúng với `evaluate`), ghi rõ `--file` bỏ file không đuôi; `loop-constraints.md` thêm 2 bullet. Chạy thật: triage có item ghi `type=noise@0.67 … priority=P1(2.01)@0.99`; Top 5 rỗng → `nothing to score`; autofix diff thật → `review` → escalated exit 1; thiếu key & key sai (401) → `unavailable` → chạy tiếp; merge thật chạy trong clone ở /tmp → ff-merge + row `merged … jev: proceed overall=proceed@0.91`. Lỗi của chính phần này: pipe mất `JEV_NOTE` → truyền bằng argument; diff qua `--file` bị `DOC_EXT` bỏ → "no documents" (exit 2) → dùng stdin; `grep \| tail` + pipefail giết runner → `awk`; trộn stdout/stderr → tách; placeholder `- (empty …)` lọt guard → sửa. `npm test` 4 suite (9 + 162 + 8 + 24). |
| 2026-09-21 16:51:53 UTC | manual-loop-tests (user-requested) | success | Harness 24 check ở /tmp nay nằm trong repo: **`scripts/run-loop-tests.mjs`** (10 check, ~9s) nối vào chuỗi `npm test`. Chạy trong **clone tạm** (copy working tree của `scripts/`, `skills/`, `loop-constraints.md`, `STATE.md`, `loop-run-log.md`, `issue-triage-state.md` ⇒ test đúng bản đang sửa, không phải bản đã commit) với shim PATH `opencode`/`npm`/`node`; shim `node` đếm mọi lần gọi jev và chỉ trả lời canned (`JEV_FAKE`), không có canned answer thì exit, và jev thật (chỉ khi `JEV_REAL=1`) luôn chạy sau fetch trap ⇒ suite không tốn request, không cần mạng. Ca: proceed → merge trong clone + dọn branch/worktree; implementer tự commit → verifier vẫn nhận diff có file; review **và** stop → escalated + exit 1 + main không đổi + branch giữ + dry-run in diff; branch rỗng → 0 call + row `nothing to judge`; thiếu key / key sai → `unavailable` ghi row, không chặn; triage → 1 call + row score@confidence, Top 5 rỗng → 0 call; `evaluate` không tài liệu → exit 2 + **không request** (kèm positive control chứng minh trap bắn). Assert cuối: repo thật không bị ghi row/worktree/branch. Lỗi của chính suite: shim khớp sai nên case đầu rơi vào jev thật → khớp `*jev.js*` + chốt exit; hai lần trong cùng giây trùng tên branch → 255 → `reset()` vào trong `run()`; assert "1 worktree" sai vì Freebuff có worktree riêng → chỉ assert không sót `wt-loop-fix-*`. **6/6 đột biến bị bắt** (bỏ gate, chỉ chặn rc=1, diff trước commit, branch rỗng vẫn gọi, unavailable chặn, bỏ `jev_triage_note`), production restore byte-identical. `npm test` 5 suite (9 + 162 + 8 + 24 + **10**). |
| 2026-09-21 17:00:29 UTC | manual-loop-seam (user-requested) | success | Bịt nốt khoảng trống: suite cũ **stub jev.js** nên runner chỉ được test bằng câu trả lời do chính suite phát ra — đổi format `--summary`/tên trường token bên jev.js thì loop vỡ mà 10 check vẫn xanh. Mở `TYPESAFE_API_URL` trong `scripts/lib/systemone.js` (unset = production như cũ; ghi rõ trong JEV.md là chỉ cho test), thêm case chạy **client thật** trước HTTP server loopback (127.0.0.1:0) trả lời theo hình dạng API và theo **đúng câu hỏi nhận được** (score→score, choice→choice) nên đổi rubric không phải sửa stub; phải dùng `spawn` bất đồng bộ vì `spawnSync` deadlock với server cùng tiến trình. Khẳng định: đúng 1 request, `Bearer test-key`, path `/v1/systemone`, `state` = diff đang chấm, `questions.overall` tồn tại, row mang `proceed overall=proceed@0.95` **và `in=4242`**, gate vẫn merge. **4/4 đột biến mới bị bắt** (bỏ `in=`, `usage`→`billing`, `answers`→`responses`, bỏ `--summary`/`2>"$err"`), production restore byte-identical. Sự cố: `mv` của sed làm mất bit `+x` của `loop-run.sh` (mode 100644) → phát hiện qua `git status`, `chmod +x`, diff về 0. `npm test` 5 suite (9 + 162 + 8 + 24 + **11**). |
| 2026-09-21 17:11:53 UTC | manual-real-cycle (user-requested) | success | **Một chu kỳ loop thật** với key trong `.dev.vars` (0600, không in giá trị), trong clone tạm cạnh repo (không ghi gì vào repo thật), chỉ `opencode` stub. Triage Top 5 thật → `jev: type=task@0.90 area=infra@0.69 priority=P1(1.83)@0.82 escalate=no@0.47! …`; autofix `--dry-run` trên diff thật → `review overall=review@0.35 … in=941` → escalated, exit 1, branch/worktree giữ, main không đổi. Đối chiếu API thật vs stub: `usage.input_tokens` thật, `score`→`{score,confidence,legend{index→label},probabilities}`, `choice`→`{choice,confidence}`, `noul`→`{noul}` (không `confidence` — nhánh cũ đúng), batched 2 doc = 1 request + `usage` top-level + kết quả riêng từng doc ⇒ **không khác biệt**, không phải sửa cách đọc. Chỉ bịt đường im lặng mất số: thiếu `usage` thì in `in=?` (test ghim bằng stub trả lời thiếu `usage`). Test: clone `--shared` (152K thay vì 33M, vì đĩa 100% đầy gây ENOSPC), `execAsync` (spawnSync + stub cùng tiến trình deadlock), gom `stubServer`/`answerAll`. Chưa sửa: `loop-run.sh` rò 1 file `loop-diff.*` mỗi lần merge. `npm test` 5 suite (9 + 162 + 8 + 24 + **12**). |
| 2026-09-21 18:04:34 UTC | manual-feature+perf (ad-hoc, user-requested) | success + merged + pushed (b996875, 19c3ab4) | **Đăng bài đa nền tảng** (việc còn dở trong worktree Freebuff `4b09be69`, chưa commit) + **dùng Jev rẻ/nhanh hơn**. FEATURE: commit `b996875` (cherry-pick sang main) — `project_channels` (migration 010, additive), fan-out mọi kênh bật khi publish, adapter điều biến per-channel (X 280 weighted URL, Threads 500, IG photo-only + link-in-bio, trim tiếng Việt theo ranh giới từ), publisher Instagram/Threads/X (OAuth 1.0a HMAC-SHA1 WebCrypto), OAuth state mang channel (state 3 phần cũ vẫn hợp lệ), token vào vault (`THREADS_TOKEN__<pid>`, `X_*__<pid>`) không vào `config_json`, API `/api/admin/projects/channels` (adminGate + audit mọi write) + `ChannelCards`, Social lọc theo kênh. Verify trên main sau merge: `npm test` **230 check** (9 + **176** + 8 + 24 + 13), `build:functions` OK, `admin:build` OK; bundle `functions_dist` rebuild chỉ khác hash chunk của esbuild (216 dòng vô nghĩa) nên đã revert. JEV (commit `19c3ab4`): khối câu hỏi 1868→**1577 ký tự (−15.6%)**, `evaluate` bỏ file sinh ra (`functions_dist/`, `package-lock.json`, `node_modules/`, `dist/`, `build/`) khỏi walk **và** khỏi diff qua stdin — đo trên chính diff này **381 700 → 118 651 ký tự (−68.9%)** với mọi file nguồn vẫn được chấm; câu hỏi batched thôi lặp tên tài liệu. Gate thật trên diff đa nền tảng: `review overall=review@0.86 … rule_fit=1.88@0.82 reuse=1.92@0.88 in=10040 — low confidence: security, scope` (exit 1) — **điểm đều trên ngưỡng, chỉ thiếu tự tin vì diff bị cắt ở 30k ký tự**; verify trực tiếp chiều `security` (JEV.md: Jev phán, không kiểm chứng): 2 endpoint mới gọi `adminGate`, callback OAuth dùng state ký (`verifyState`), migration additive, secret qua `setVaultSecret`, `audit()` 5 chỗ, không `console.log` token ⇒ không có phát hiện. `npm test` main **230** + loop suite **13** (thêm 1 case ghim state gửi lên stub). Còn nợ: `loop-run.sh` rò 1 file `loop-diff.*` mỗi lần merge (từ pass trước). |
| 2026-09-21 18:13:56 UTC | manual-deploy (ad-hoc, user-requested) | success + deployed | Deploy đa nền tảng lên production. `npm run build` (check-public + bundle-schema + admin:build) không gây drift (`functions/_lib/schema.js` khớp init.sql). `npx wrangler pages deploy public --project-name=gu-seo` → **https://b9e764c2.gu-seo.pages.dev**, live tại seo.gulagi.com. **Migration**: `npm run migrate -- --dry-run` lộ 4 migration chờ chứ không phải 1 — **007_video_jobs, 008_video_brand, 009_video_website chưa từng được áp lên D1 prod** (chỉ 6/10 đã áp). Đã hỏi và được duyệt áp cả 4; tất cả additive (`CREATE TABLE/INDEX IF NOT EXISTS`, `ALTER TABLE ADD COLUMN`), không DROP/DELETE/UPDATE. `npm run migrate` → 10/10, `007` 3 câu + `010` 3 câu (bảng + 2 index), `008`/`009` báo **0 câu** — kiểm chứng bằng `pragma_table_info`: cột `video_tagline/brand_accent/address/phone` và `video_jobs.kind/source_url` **đều đã có**, tức baseline `schema/init.sql` đã mang chúng và runner coi `duplicate column name` là skip (`BENIGN`) — đúng, không phải lỗi im lặng. Verify live: `/api/health` ok (232 bài), `/api/admin/projects/channels` trả **401** (đã deploy + `adminGate`), chunk `chunks/Publishing-y1otrgFV.js` (khớp hash build local) chứa `projects/channels`, `channels-connect?channel=facebook\|threads\|instagram`, form 4 credential X, hashtag Instagram, thẻ "Đa nền tảng / N kênh đang bật". cron-worker không đổi nên không deploy lại. Ghi nhận: `/api/health` báo `jobs.in_flight_stuck: 1` (job video kẹt — chưa xử lý, ngoài phạm vi). |
| 2026-09-21 18:25:30 UTC | manual-fix (bug do người dùng báo) | success | **"Rê chuột vào video bị ẩn mất button xoá"**. Nguyên nhân (đo, không đoán): `@rc-component/table` ép **cả bảng** sang `table-layout: fixed` khi **bất kỳ** cột nào có `ellipsis` (`Table.js` dòng 429-432) — bảng Video có 2 cột như vậy. Cột "Hành động" rộng **220px** nhưng 4 nút cần **232px + 32px padding ô = 264px**, nên nút xoá (phải cùng) tràn sang ô kế bên; nền ô kế bên — chỉ được tô khi **hover hàng** (`rowHoverBg`) — phủ lên nó. Tức nút luôn tràn, chỉ *lộ ra* khi không hover. Dựng lại thật: bundle `Video.jsx` bằng esbuild + Chrome headless (`--screenshot`/`--dump-dom`) với đúng bộ cột, thấy rõ ca 220px+hover mất nút còn 280/300 thì không; số đo `content needs=232, padding=32 → min 264`. Sửa: `dataIndex: 'video_url'` → `key: 'actions'`, `width: 220` → **300** (kèm comment giải thích ràng buộc). Thêm guard vào `run-admin-hook-tests.mjs` (đọc source, assert width ≥ 264) — đã kiểm chứng bằng đột biến: đặt lại 220 thì suite **fail đúng chỗ**. `npm test` **231** (admin hook 8→9), `admin:build` OK. Đã dọn thư mục repro tạm. Carousel (3 nút) cần 266px, đang 280px → *vừa đủ*, chưa hỏng nhưng biên mỏng 14px (ghi nhận, không sửa vì ngoài phạm vi). |
| 2026-09-21 18:29:57 UTC | manual-deploy (bug fix, user-approved) | success + deployed | Deploy bản sửa nút xoá (chỉ đổi UI, không migrate). `npm run build` → `npx wrangler pages deploy public --project-name=gu-seo` → **https://84a64f81.gu-seo.pages.dev**. Verify live (không chỉ tin output): `main.js` trên seo.gulagi.com trỏ `chunks/Video-CUAshxnK.js`, tải chunk đó về (HTTP 200, 8924 bytes) và thấy `key:\`actions\`,width:300` — đúng bản đã sửa. `/api/health` ok (232 bài), `/admin/` HTTP 200. Ghi chú: wrangler báo "Uploaded 0 files (143 already uploaded)" (dedupe theo nội dung) nhưng chunk hash mới đã được phục vụ ⇒ vẫn phải verify bằng nội dung, không suy ra từ con số upload. |
| 2026-09-21 18:38:46 UTC | manual-diagnosis (bug do người dùng báo) | success (không cần sửa code) | **"Chưa có nhạc nền trong video được tạo"** — KHÔNG phải lỗi còn sót: code đã đúng, chỉ là video cũ được render trước khi bản sửa lên VPS. ĐO chứ không suy luận: (1) file đã phát hành `video/gulagi-1790006679887.mp4` → `silencedetect -50dB` ra **9 khoảng lặng** (có cái dài 4.8s) và `I = -16.8 LUFS` — tức bed không lấp gì cả; (2) VPS `node2-prod` chạy `render-video.mjs` **trùng sha256 local** (`c31a2879…`), bản render gần nhất bằng code mới (16:12 UTC) → `-45dB` chỉ còn **1** khoảng lặng (27.6→28.65s, đuôi fade-out cố ý) và `I = -14.6 LUFS` — bed lấp mọi khoảng lặng; (3) file backup `.bak-7a3aa1e-…` trên VPS chứng minh code cũ: `amix=3` (chia 3, mất 9.5 dB) + sine 0.16/0.22/0.18 + `lowpass=900` + **không có** `masterLoudness`, so với code mới `amix=inputs=3:normalize=0` + 0.30/0.34/0.30 + `lowpass=2400` + có master. Bed cũ ~**-60 LUFS** (dưới ngưỡng nghe) và `-16.8` của file người dùng khớp đúng bản code cũ. MỐC THỜI GIAN: video gulagi phát hành **16:04:40**, VPS nhận code mới **16:09**, render đầu bằng code mới **16:12** ⇒ tất cả video hiện có đều render trước bản sửa. Người dùng chọn tự bấm trong admin (business: nút "Video doanh nghiệp" — endpoint tự xoá row `done` rồi insert `pending`; post: nút xoá video trước rồi "Render tất cả bài thiếu", vì `enqueue-missing` chỉ nhận bài `NOT EXISTS` row). NGUYÊN NHÂN GỐC CẦN XỬ LÝ: **`video-agent/` không có script deploy** — code copy tay lên VPS, nên bản sửa nằm im trên repo trong khi VPS chạy bản cũ. Ghi nhận thêm: `/api/health` báo `jobs.in_flight_stuck: 1` nhưng mọi row `video_jobs` đều `done` — chưa truy ra health đếm gì. |
| 2026-09-22 07:01:17 UTC | manual-feature (ad-hoc, user-requested) | success + released + deployed (7034810, 70e6fec, 8d5ea7b, 64595ce, fbc4a1f, e6a6d31, 47e14bf) | **Video kể chuyện theo intent** — spec người dùng gọi đúng tên thứ vừa làm ở v1.20.0: `hook → bars → steps → icons → quote → outro` là **anti-pattern #17**. Rework toàn bộ tầng storyboard. (1) **`video-agent/storyboard.mjs`** — 9 intent (product_demo, local_business, educational, listicle, storytelling, announcement, testimonial, before_after, product_promotion), mỗi intent một beat template (beat → trọng số thời lượng + đúng bộ scene type được phép). Intent phân loại **không cần model** (`intentFromSignals`: có source_url → product_demo; có địa chỉ/business_type → local_business; "5 bước" → listicle; có số liệu + how-to → educational). (2) **ĐẢO NGƯỢC THỜI LƯỢNG** — storyboard chốt slot trước (mặc định 20s, trần 45s), `fitNarration` đo TTS rồi nếu cảnh nào vượt slot >15% thì **cắt lời đọc + đọc lại một lần** (chạy thật: segment 3.4s → 2.6s). (3) **Luật trong `sanitizeStoryboard`**: text ≤8 từ, loại cảnh phải nằm trong bộ của intent (bars trong local_business bị từ chối), không 2 cảnh liên tiếp cùng loại, số phải có trong nguồn, asset phải tồn tại thật. (4) **`reviewStoryboard`** = quality gate §16: từ chối nếu phần thân chủ yếu là chữ không có gì để nhìn, hoặc product_demo mà không dùng asset thật; hook + CTA không bị tính là slideshow. (5) **`video-agent/assets.mjs`** — collectAssets theo intent (chỉ trả tiền chụp site khi intent cần), Maps chụp bằng Chrome sẵn có (không API key), mockup điện thoại, before/after. (6) **`video-agent/scenes.mjs`** — cảnh hình ảnh mới (ui_demo trong khung điện thoại + cuộn, before_after, location, rating, product_reveal, result, feature, problem, photo, cta) + đồ hoạ cũ (chỉ dành cho educational/listicle); mọi cảnh **hạ cấp thay vì vỡ** (ui_demo không có ảnh → hiện claim). (7) `explainer.mjs` giải thể; 3 composition gộp thành `composeStoryboardHtml`; shell có motion theo từng cảnh (zoom/pan/scroll/reveal) và timeline nay index theo meta (trước đây index theo sceneHtml nên animate cả id không tồn tại). **Nghiệm thu THẬT trên VPS**: job `website` gulagi.com → `intent product_demo`, **6 cảnh/20s**, assets `hero, site:0, site:1, photo:0..2`, khớp TTS chạy đúng, master -14 LUFS, video **20.6s** (trước là 50–79s). Lần render thật lộ **3 lỗi mà đọc code không thấy**: `captureSite` chụp 2 link nav đầu tiên → khung điện thoại hiện **trang chính sách bảo mật** (sửa: chọn link theo nhãn — menu/dịch vụ/bảng giá, bỏ policy/login/tag); `captureSite` chạy **2 lần/job** (renderOne + collectAssets) → nhân đôi bước chậm nhất (sửa: collectAssets trả luôn siteText); fallback sinh 2 `ui_demo` liền nhau nên luật đa dạng **ăn mất một beat** (sửa: rebuild thành type khác cùng beat — 6 cảnh vẫn 6). **Test 41 check** (video-agent) / **249 toàn suite**; **11/11 đột biến bị bắt** — 2 cái đầu bị sót là **thiếu test thật** (không có ca `stat` bịa số, không có ca `say` vượt ngân sách) nên đã thêm. Release v1.21.0 + tag; agent deploy bằng `deploy.sh` (sha256 khớp 4 file). Ghi nhận: **GuRouter hết credit** trên production (400 insufficient credits) — fallback tất định gánh toàn bộ, và nó cho ra storyboard hợp lệ; cần nạp credit để có bản do LLM viết. |
| 2026-09-22 07:35:25 UTC | manual-fix (bug do người dùng báo) | success + deployed (cd1a01c) | **"Đã có job đăng" khi bấm Đăng Facebook**. Nguyên nhân là một **mâu thuẫn nằm ngay trong code**: `UNIQUE(blog_post_id, channel)` khiến "một row cho mỗi (bài, kênh)" là vĩnh viễn, và `enqueueSocialPost` dùng `INSERT OR IGNORE` — nên row ở **bất kỳ** trạng thái nào cũng chặn insert, kể cả `published`. `deliver.js` viết rõ trong comment *"a missed enqueue is recoverable via the manual button"*, nhưng nút thủ công chịu đúng cái index đó ⇒ **không bao giờ** chạy được. Đo trên production: `facebook_video` có **9 `published` + 1 `skipped`, 0 row nào đang chạy** — tức mọi video đã từng đăng đều bị chặn. Sửa: `enqueueSocialPost` nhận cờ `repost`; khi bật, row ở trạng thái kết thúc (`published`/`skipped`/`failed`) được reset về `pending` với attempts=0 và **xoá link cũ** (external_id/url/published_at — row mô tả lần thử hiện tại), còn row đang thật sự trong hàng đợi (`pending`/`publishing`) vẫn bị từ chối. **Chỉ nút thủ công** truyền cờ này; fan-out tự động giữ nguyên tính idempotent nên publish lại bài không thể đăng trùng. Câu chữ admin đổi thành "đang được đăng" — giờ đúng là trường hợp duy nhất API từ chối. Kiểm chứng: platform test **chạy SQL thật trên SQLite thật** (`node:sqlite`, cùng họ engine với D1) nên cú pháp `ON CONFLICT … DO UPDATE … WHERE` được chứng minh hợp lệ **mà không phải poke production** (poke sẽ đăng thật lên Facebook); đột biến `WHERE 0` bị bắt đúng chỗ. Test mới để lại row `pending` làm hỏng test "cancelled job must not be drained" ở sau (drain quét theo **cả project**) → đã trả fixture về `published`. Lỗi thứ hai lộ ra khi chạy `npm test` **không qua pipe**: `npm test | grep` che exit code, nên tôi tưởng xanh trong khi suite admin hook đang đỏ vì câu chữ cũ — sửa test. `npm test` **254 check** (9 + 181 + 9 + 42 + 13), exit 0. Deploy Pages → **https://5b10d655.gu-seo.pages.dev**; verify live: chunk `VideoStatusTag-ti599Oi2.js` chứa "đang được đăng" và `main.js` trỏ đúng chunk đó. |
| 2026-09-22 07:48:36 UTC | manual-feature (ad-hoc, user-requested) | success + deployed (06cfc55, dd489b5) | **Thêm xoá cho Programmatic SEO**. Trang chỉ có ưu tiên / bỏ qua / thử lại — **không có đường xoá nào** cho `prog_keywords` lẫn `prog_pages` (grep `DELETE FROM prog` ra 0), nên từ khoá rác/trùng nằm lại vĩnh viễn và row `done` không có gì để bấm. Đã hỏi và chốt phạm vi: **chỉ xoá row khỏi hàng đợi**, KHÔNG xoá trang — vì `functions/p/[slug].js` phục vụ `prog_pages`, tức trang đó là nội dung đang sống (có URL + entry sitemap); xoá nó là quyết định phá hoại khác mà operator chưa đưa ra. `DELETE /api/admin/prog/queue?id=…` trả luôn `page_slug` để UI nói rõ "trang /p/x vẫn giữ nguyên", và confirm hiện câu đó TRƯỚC khi bấm. Row đang `processing` bị từ chối 409 thay vì xoá dưới tay generator. **Cột hành động 150px là bẫy đã biết**: bảng keyword có cột `ellipsis` ⇒ `@rc-component/table` ép `table-layout:fixed`, nên nút thứ tư sẽ tràn sang ô kế bên — đúng cách trang Video mất nút xoá. **Đo bằng antd thật** (esbuild + Chrome `--dump-dom`): row `pending` cần **132px + 16px padding = 148**, cột cũ 150 chỉ dư 2px ⇒ nâng lên **200**; admin hook suite ghim ngưỡng 148. Kiểm chứng bằng đột biến: xoá luôn `prog_pages` bị bắt đúng ở "the page the keyword produced is NOT deleted". `npm test` **257 check** (9 + 183 + 10 + 42 + 13), exit 0. Deploy Pages → **https://d67b2567.gu-seo.pages.dev**; verify live: `DELETE` trả **401** (endpoint có thật + có gate, không phải 404/405), `PATCH` vẫn 401 như cũ, chunk `Prog-DyHZhzdM.js` chứa câu xác nhận. PHÁT HIỆN PHỤ: `functions_dist/index.js` đã commit **bị cũ** (0 lần xuất hiện `onRequestDelete`) vì `npm run build` không rebuild bundle Functions — đã chạy `build:functions` và commit lại. SỰ CỐ MÔI TRƯỜNG: đĩa đầy 100% (còn 208–228 MB) làm suite video-agent **ENOSPC**; đã xin phép xoá worktree Freebuff `.freebuff` (414 MB, nhánh đa nền tảng đã cherry-pick sang main + push từ trước) — `git worktree remove --force` **giữ nguyên branch** `freebuff/t-…-4b09be69`, đĩa lên 612 MB. Lưu ý: `npm test | grep` che exit code của npm (đã dính một lần ở pass trước) nên lần này chạy `out=$(npm test)` rồi đọc `$?`. |
| 2026-09-23 15:21:27 UTC | manual-fix (user-reported bug: "cron không hoạt động") | success + deployed | Hai cron chết: (1) timer video trên VPS `disabled` và CHƯA TỪNG fire (`OnUnitActiveSec` không lên lịch khi không có mốc kích hoạt cuối — `NEXT: n/a` mãi) → viết lại `OnCalendar=*:0/15` + `Persistent=true` trên VPS; verify sống: fire 22:08/22:15, render 3 video xong (1 bản mastered -14 LUFS), timer tự re-arm `NEXT 22:30 waiting`. (2) `in_flight_stuck` = 21 — mỗi project một job `created` sinh 01:03–01:10 UTC, `error NULL`: đợt fan-out 01:00 (39 project) chết giữa cửa sổ trên scheduled invocation (~15 phút), còn `/blog/text` chỉ ghi row SAU khi model trả lời nên chết im không để lại gì. Phục hồi bằng tick per-project với token admin lấy từ D1 (`settings.admin_token`, token `.dev.vars` là dev nên 401): **20/21 publish**; job cuối (`usasglobal`) là nhân bản thật — project đã có bài hôm nay nên tick trả `skipped: already_run_today`, row cũ giữ `created` để counter phản ánh đúng. CODE: cron worker thêm lưới cứu mồ côi lúc **:40** (sau cửa sổ fan-out ~17 phút; :10/:15/:30 sẽ xử lý đúp chuỗi đang chạy), một call không lọc — blogChain resume sẵn làm no-op ngày lành; `FANOUT_CONCURRENCY` 4→6; nhánh resume `created` trong tick.js giờ mang `resumed:true`. Ghim test mới trong `D. Cron schedule routing`: lưới chỉ fire :40, ĐÚNG MỘT call không lọc, không fire :45. `npm test` **282 check**, rc=0 (đọc `$?` của npm, không qua pipe). |
## Next actions

- [ ] First `loop-triage` run to baseline open issues/PRs
- [ ] Verify cron/CI hooks for `ci-sweeper` (15m) and `pr-babysitter` (30m)
- [ ] Weekly cost report check per `loop-budget.md`
