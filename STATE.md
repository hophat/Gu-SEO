# Loop State — Gu-SEO

Tracker for loop engineering runs. Updated by each loop invocation.
Budget and cadence: see `LOOP.md` + `loop-budget.md`. Safety: see `gate.yaml`.
Binding rules: `loop-constraints.md` (loaded before every run).
Mode: `report-only` (L1, propose only) — flip to `mode: auto-fix` in `loop-constraints.md` after 10 stable runs to enable L2. `loop-pause-all` here stops all loops.

## Active patterns

| Pattern | Cadence | Risk | Readiness | Status |
|---|---|---|---|---|
| issue-triage | 2h–1d (`scripts/loop-run.sh issue-triage`) | low | L1 | active — reads/writes `issue-triage-state.md` |
| daily-triage | 1d (09:00 UTC) | low | L1 | active — merges issue-triage Top 5 into High Priority |
| pr-babysitter | 30m (business hours) | medium | L2 | active |
| ci-sweeper | 15m | high | L2 | active |
| dependency-updater | 1d | medium | L1 | active |
| code-reviewer | on_demand | low | L2 | active |
| test-runner | on_demand | low | L1 | active |
| documentation-sync | 1d | low | L1 | active |

## Budget (from loop-budget.md)

- Daily token cap: 100,000 — monthly: 3,000,000 — per-loop: 10,000
- Kill switch: enabled (daily cap exceeded / 5 consecutive failed loops / provider rate limits). Auto-recovery: disabled, manual intervention required.
- Cost: $50/day hard stop, alert at $40/day. Alert at 80% of daily tokens. Weekly cost reports.

## Gate summary (from gate.yaml)

Denied paths: `schema/init.sql`, `functions/_lib/schema.js`, `functions/_lib/auth.js`, `wrangler.toml`, `.env`, `.env.local`, `package.json`, `node_modules/`.
Human approval required for: database changes, auth changes, production deploys. Auto-merge: enabled in `auto-fix` mode — only `loop/fix-*` branches whose verifier APPROVEd and whose tests + build passed, fast-forward only; denylisted paths and REJECTs still escalate.

## Run log

Format: `[YYYY-MM-DD HH:MM:SS UTC] {pattern-name} - {status} - {details}`
(Full history: `loop-run-log.md`.)

| Timestamp (UTC) | Pattern | Status | Details |
|---|---|---|---|
| 2026-09-16 17:54:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed | Brand admin fix e783b26: theme-color/logo UI added to Brand.jsx, save strips logo_url (fixes logo_url_is_server_assigned), voice_tone/target_audience mapping. Gate ✅. Pushed main, deployed https://51e130b6.gu-seo.pages.dev. Left uncommitted (not mine): App.jsx menu move, wrangler.toml name. |
| 2026-09-16 18:05:00 UTC | manual-feature (ad-hoc, user-requested) | success + deployed (2fb90cc) | Calendar "Tạo bài ngay": per-slot button (scheduled/draft only) in drawer runs blog chain start(calendar_slot_id)→text→image→publish with progress modal. Backend reused as-is (start claims slot, publish marks published). Files: src/admin/pages/Calendar.jsx (+rel=noopener fix). Gate ✅. Pushed main, deployed https://d78cf961.gu-seo.pages.dev. |
| 2026-09-16 18:15:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (25f4ed1) | Move "Bài đăng mạng xã hội" menu item from Phân phối group to Bài viết group (src/admin/App.jsx, pre-existing local change). Gate ✅. Pushed main, deployed https://81f6ab34.gu-seo.pages.dev. Still uncommitted (do-not-commit): wrangler.toml name. |
| 2026-09-17 09:30:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (95b7fe6, 7c65575) | Custom-domain approval 502 root-caused: `CF_API_TOKEN` was valid but missing `Cloudflare Pages: Edit` → 403 code 10000 on `pages/{project}/domains`. Deleted 4 empty secrets that caused the earlier misdiagnoses (`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_KEY`, `CLOUDFLARE_EMAIL`), filled `CF_D1_ID` + `CF_R2_NAME`, reverted the dead global-key fallback in `cloudflare_domains.js`, added an attach-failure log (hostname + CF error, no secrets) in `domains/requests.js`. Approved `blogs.gurouter.com` via wrangler OAuth attach + D1 flip + `audit_log` row, verified live; token later granted `Pages: Edit` and approve returned 200 (`cf_status: initializing`). Temp diagnostic `cf-check.js` added then removed. Gate ✅. |
| 2026-09-17 15:00:00 UTC | manual-feature (ad-hoc, user-requested) | success + deployed (1f70cec) | Brand language selector controls AI output language. `projects.language` is the single source of truth: `brand-dna` GET returns it and PUT validates against a 20-code allowlist (the field lands in the prompt, so free text was a prompt-injection vector); `ai.js` resolves it per `projectId` inside `generateContent`, so blog/programmatic/refresh all obey it, and `preview-sample` was missing `projectId` so previews now match too. UI select in `Brand.jsx`. E2E: `language=en` → 2694-word English preview with 0 Vietnamese diacritics; injection attempt → 400. Gate ✅. Pushed main, deployed https://b4079176.gu-seo.pages.dev. |
| 2026-09-20 11:00:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (cron-worker) | Cron looked dead (newest post 65h). Root-caused with Workers analytics: every 01:00 invocation used **exactly 50 subrequests** — the free-plan per-invocation cap — because the standalone social sweep spends 1 dry_run + 1 call per active project (~30), leaving ~19 for the blog fan-out. `listProjects` orders `created_at DESC`, so the tail of the list — the 10 oldest projects incl. `gulagi` + `gurouter` — was starved for 3 days while the 19 newest published fine. Fix: social is now **one unfiltered tick call** (`runTask(..., { all: true })`); blog/prog/refresh keep per-project fan-out (their chains exceed the edge timeout, social's do not). Blog hour is now 1 + 1 + 29 = 31 ≤ 50. `observability` enabled so a partial fan-out is no longer invisible. Verified: 10:30 tick = 1 subrequest (was 29–30); a forced 11:00 run via a temporary `BLOG_HOUR` secret published all 10 starved projects (`/api/health` posts 178 → 188), secret deleted after. Cron test updated (social = 1 call, blog = per-project). |
| 2026-09-20 14:00:00 UTC | manual-fix (ad-hoc, user-requested) | success + deployed (68a86c31, 187782d5) | Facebook channel: "Token Facebook không hợp lệ … Tạo Page Access Token mới" on publish, and reconnecting never listed the already-connected Page. Two repo defects, both hiding the real cause: `listManagedPages` **silently dropped** Pages Meta returns without a per-Page token (→ `no_pages` → "Không tìm thấy Page nào"), and `describeGraphError` collapsed Graph **code 190** into one message although it also means "token belongs to a different Page" and subcode **492** ("user has no appropriate role on the Page"); the same failure can arrive as **100/33**. Fixes: keep + flag token-less Pages; precise `no_token` / `select_page` outcomes; on 190 or 100:33 ask `/me` with the token itself to name which Page it belongs to (wired into `verifyFacebookPage` + both publish paths); `configured_page_seen` makes the picker warn when the configured Page is absent from Meta's list, and the empty state distinguishes "list expired" from "Meta returned no Pages". Research: the string is undocumented by Meta but maps to 190 / 100:33 in vendor docs; the account-side remedy (Page role or a different login) stays the operator's. 152 platform checks pass (3 new FB tests + cron routing realigned). |
| 2026-09-21 08:51:09 UTC | manual-fix (ad-hoc, user-requested) | success + merged (b54c309) | Carousel lifecycle after the publish fix: delete.js lists the carousel prefix and removes only `<prefix>-N.png` slides (a bare-prefix list also matched a neighbouring slug's slides — caught by exercising, not skimming); list.js strips the carousel sentinel in the blog_posts join so rows show the article title, and drops the dead `isCarousel` var. Built in a Loop worktree `loop/fix-20260921154352`: npm test 154 checks pass + build:functions compiled, ff-merged to main, worktree/branch removed. Also flipped loop-constraints.md to `mode: auto-fix` and taught scripts/loop-run.sh to ff-merge an APPROVEd fix and log to loop-run-log.md + STATE.md. |
| 2026-09-21 08:58:19 UTC | manual-config (ad-hoc, user-requested) | success | Added `autofix --dry-run` (or `LOOP_DRY_RUN=1`) to scripts/loop-run.sh: implementer + verifier + `npm test` + `build:functions`, prints the diff, logs the run, keeps the `loop/fix-*` branch/worktree for a human — never merges. Testing surfaced two runner blockers: `pause_check` matched the literal `loop-pause-all` so STATE.md's prose mention exited EVERY run; `mktemp /tmp/loop-diff.XXXXXX.patch` (non-trailing X) collided on the second run — now `${TMPDIR:-/tmp}/loop-diff.XXXXXX`. Verified with a stub `opencode`: main unchanged at b54c309. |
| 2026-09-21 09:40:53 UTC | manual-feature (ad-hoc, user-requested) | success + merged + deployed (9efe1e7, 51035e6) | Carousel tách khỏi trang Video thành trang riêng `#carousel` (nhóm Bài viết): chọn bài viết bằng dropdown có tìm kiếm thay vì gõ slug, hướng dẫn 4 bước, nhãn vai trò từng slide (Bìa/Ý/Kêu gọi), xem trước cả bộ 5 slide, tự làm mới khi agent đang render. Files: src/admin/pages/Carousel.jsx (mới), src/admin/App.jsx (menu + route), src/admin/pages/Video.jsx (gỡ carousel, lọc kind !== 'carousel', dọn dead code). Kèm 51035e6: timeout guard cho opencode trong loop-run.sh (LOOP_AGENT_TIMEOUT) + seed issue thật vào issue-triage-state.md. `npm run admin:build` OK. Pushed main, deployed Pages https://e6042544.gu-seo.pages.dev (cron-worker không đổi). Verified live: main.js trỏ Carousel-B--e-QvJ.js và chunk chứa copy mới (HTTP 200). |
| 2026-09-21 10:36:00 UTC | manual-test (user-requested) | success | Smoke test đường render carousel của agent, không cần Chrome/hyperframes/network. Hợp đồng deck: 5 slide 1080×1350 (cover / 3 điểm / CTA), pad đủ 5 slide, accent theo brand + fallback, escape text, HTML tất định, `slideQueries` riêng từng slide. Đường render thật: `renderCarousel` chạy với workspace tạm + `spawn`/`photo`/`deliver` giả — 5 snapshot đúng thứ tự deck (mỗi cái render đúng slide + ảnh của mình), deliver đúng `job_id` + 5 slide đúng thứ tự (khớp `carousel/<slug>-1..5.png`), GuRouter tắt/host ảnh chết vẫn đủ deck, snapshot fail/no-PNG dừng trước deliver, deliver non-2xx hoặc 200-không-thành-công đều fail job. Seam tối thiểu `renderCarousel(job, deps)` + `assetsDir`, batch loop gate theo entry point (gọi trực tiếp vẫn claim như cũ). 4 đột biến đều bị bắt. `npm test` = 4 suite (9 + 157 + 8 + 14). |
| 2026-09-21 10:19:06 UTC | manual-test (loop follow-up, user-requested) | success | Đưa 4 ca của pass verify thành test thường trú thay vì probe ở /tmp. Platform suite thêm mục Q (R2 + Graph giả): carousel trên kênh bật `as_video` upload đủ photo/0 lần `/videos`, `listSocialPosts` trả `post_title` đúng cho job carousel, delete xoá đúng slide của slug mình và giữ slug láng giềng. Script mới `scripts/run-admin-hook-tests.mjs` (+ harness React hook tối thiểu) chạy thật `videoQueue.js` với fetch/timer giả: Video không poll, Carousel poll 20s rồi tự tắt. `npm test` = 3 suite (9 + 157 + 8 check). Xác nhận test bắt được lỗi bằng 4 đột biến — cả 4 fail đúng chỗ; không phải sửa production code. |
| 2026-09-21 10:10:46 UTC | manual-verify (architect pass follow-up, user-requested) | success | Nghiệm thu refactor bằng cách chạy thật. 3 probe, 52 check pass: vòng đời carousel qua đúng handler trên node:sqlite + R2/Graph giả (20), `videoQueue.js` trên React mini-runtime + fetch/timer giả (20) — Video không poll, Carousel poll 20s rồi tự tắt, và mount thật Video.jsx/Carousel.jsx bằng esbuild của repo để chứng minh mỗi trang chỉ hiện đúng kind của mình (12). Lần chạy lộ 2 lỗi thật: `facebook.js` kiểm tra `cfg.asVideo` trước nhánh carousel nên kênh bật `as_video` làm nút "Đăng FB" của carousel hỏng (prefix `carousel/<slug>` bị đưa sang upload video); `listSocialPosts` join bằng `blog_post_id` thô nên job carousel hiện `post_title = null`. Cả hai đã sửa; npm test 154 + build:functions + admin:build đều xanh. |
| 2026-09-21 10:03:59 UTC | manual-refactor (architect pass, user-requested) | success | Kiến trúc cho code carousel/video. BACKEND: `functions/_lib/video_jobs.js` là chủ sở hữu DUY NHẤT của quy ước sentinel ref (carousel:/project:/url:) và hợp đồng slide carousel (carousel/<slug>-N.png, đúng 5 slide) — carousel, carousel-deliver, claim, list, delete, social_queue, facebook, publisher đều gọi lib. FRONTEND: `src/admin/lib/videoQueue.js` (status meta theo kind, loader + polling, publish/delete dùng chung) + `src/admin/components/VideoStatusTag.jsx`; Video.jsx và Carousel.jsx chỉ còn trình bày + luồng tạo riêng. Verify: npm test 154 pass, build:functions OK, admin:build OK (Carousel 8.68→7.41kB, Video 9.91→8.65kB). Real run: probe sqlite create→list→claim→deliver→list→social drain→delete, xoá đúng slide prefix, giữ carousel/alpha-postX-1.png. |

## Next actions

- [ ] First `loop-triage` run to baseline open issues/PRs
- [ ] Verify cron/CI hooks for `ci-sweeper` (15m) and `pr-babysitter` (30m)
- [ ] Weekly cost report check per `loop-budget.md`
