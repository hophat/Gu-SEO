# Self-hosted webfonts used by the cover SVG renderer. Long-cache
# them aggressively — they're versioned implicitly by the URL (the
# weight is in the filename) and update only when the font itself
# is replaced (rare). cross-origin CORS so the SVG can reference
# them when embedded via <img>.
/_fonts/*.woff2
  Content-Type: font/woff2
  Cache-Control: public, max-age=31536000, immutable
  Access-Control-Allow-Origin: *

# ── Security headers ─────────────────────────────────────────────
# Static-asset responses get baseline hardening. (Function responses
# set their own headers in code — _headers doesn't apply to them.)
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin

# The admin SPA and sign-in page must never be framed (clickjacking)
# and should never be cached by shared caches.
/admin
  X-Frame-Options: DENY
  Cache-Control: no-store

/admin.html
  X-Frame-Options: DENY
  Cache-Control: no-store

/sign-in
  X-Frame-Options: DENY
  Cache-Control: no-store

/sign-in.html
  X-Frame-Options: DENY
  Cache-Control: no-store

# The admin bundle. These file names never change, so a stale copy
# silently shows the wrong UI — a project admin kept seeing the System,
# Settings and Users tabs long after they were hidden for that role.
# no-store, not no-cache: this zone sets browser_cache_ttl=14400, which
# makes Cloudflare rewrite the origin's max-age (and drops no-cache) to
# 4 hours. Only no-store survives that rewrite, which is why /admin.html
# already uses it.
/admin.js
  Cache-Control: no-store

/admin.css
  Cache-Control: no-store

# The React admin bundle. main.js has a stable file name, so a stale
# copy silently serves the previous build (stale role menu, stale
# project links). Same zone-rewrite problem as /admin.js above —
# only no-store survives. Content-hashed chunks under /admin-dist/chunks
# are safe to cache; only the entry point needs no-store.
/admin-dist/main.js
  Cache-Control: no-store

# The bundle's stylesheet carries the shared design tokens the vanilla
# cover editor reads (--bg, --line, …). It has a stable name too, so it
# needs the same no-store treatment as main.js.
/admin-dist/assets/index.css
  Cache-Control: no-store

/cover-editor.js
  Cache-Control: no-store

/cover-editor.css
  Cache-Control: no-store
