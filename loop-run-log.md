# Loop Run Log

Append-only log of loop executions with timestamps and outcomes.

## Format
```
[YYYY-MM-DD HH:MM:SS UTC] {pattern-name} - {status} - {details}
```

## Log Entries

[2026-09-21 08:51:09 UTC] manual-fix (ad-hoc, user-requested) - success + merged (b54c309) - Carousel lifecycle after the publish fix: delete.js lists the carousel prefix and removes only <prefix>-N.png slides (a bare-prefix list also matched a neighbouring slug's slides — caught by exercising); list.js strips the carousel sentinel in the blog_posts join so rows show the article title, and drops the dead isCarousel var. Built in a Loop worktree loop/fix-20260921154352: npm test 154 checks pass + build:functions compiled, ff-merged to main, worktree/branch removed. Flipped loop-constraints.md to mode: auto-fix; scripts/loop-run.sh now ff-merges an APPROVEd fix and logs to loop-run-log.md + STATE.md.

[2026-09-21 08:58:19 UTC] manual-config (ad-hoc, user-requested) - success - Added `autofix --dry-run` (or `LOOP_DRY_RUN=1`) to scripts/loop-run.sh: runs implementer + verifier + `npm test` + `build:functions`, prints the diff, logs the run, and leaves the `loop/fix-*` branch + worktree for a human — never merges (every keep-for-human exit prints the diff too). Testing it surfaced two blockers in the runner: `pause_check` grepped the literal `loop-pause-all`, so STATE.md's prose mention armed the kill switch and EVERY run exited immediately; and `mktemp /tmp/loop-diff.XXXXXX.patch` (X's not trailing) returned a literal path that collided on the next run — now `${TMPDIR:-/tmp}/loop-diff.XXXXXX`. Verified end-to-end with a stub `opencode`: dry-run left main unchanged at b54c309, kept the branch/worktree, and logged a dry-run row.

[2026-09-21 09:40:53 UTC] manual-feature (ad-hoc, user-requested) - success + merged + deployed (9efe1e7, 51035e6) - Carousel tách khỏi trang Video thành trang riêng `#carousel` (nhóm Bài viết): chọn bài viết bằng dropdown có tìm kiếm thay vì gõ slug, hướng dẫn 4 bước, nhãn vai trò từng slide (Bìa/Ý/Kêu gọi), xem trước cả bộ 5 slide, tự làm mới khi agent đang render. Files: src/admin/pages/Carousel.jsx (mới), src/admin/App.jsx (menu + route), src/admin/pages/Video.jsx (gỡ carousel, lọc kind !== 'carousel', dọn dead code). Kèm 51035e6: timeout guard cho opencode trong loop-run.sh (LOOP_AGENT_TIMEOUT) + seed issue thật vào issue-triage-state.md. `npm run admin:build` OK. Pushed main, deployed Pages https://e6042544.gu-seo.pages.dev (cron-worker không đổi). Verified live: main.js trỏ Carousel-B--e-QvJ.js và chunk chứa copy mới (HTTP 200).

[2026-09-21 10:03:59 UTC] manual-refactor (architect pass, user-requested) - success - Kiến trúc cho code carousel/video. BACKEND: `functions/_lib/video_jobs.js` giờ là chủ sở hữu DUY NHẤT của quy ước sentinel ref (carousel:/project:/url:) và hợp đồng slide carousel (carousel/<slug>-N.png, đúng 5 slide) — carousel, carousel-deliver, claim, list, delete, social_queue, facebook, publisher đều gọi lib thay vì tự mã hoá lại. FRONTEND: `src/admin/lib/videoQueue.js` (status meta theo kind, loader + polling, publish/delete dùng chung) + `src/admin/components/VideoStatusTag.jsx`; `Video.jsx` và `Carousel.jsx` chỉ còn phần trình bày và luồng tạo riêng (bỏ ~60 dòng trùng mỗi trang). Verify: npm test 154 pass, build:functions OK, admin:build OK (chunk Carousel 8.68→7.41kB, Video 9.91→8.65kB). Real run: probe sqlite chạy create → list → claim → deliver → list → social drain → delete, xác nhận title/ref/slide URL đúng và xoá chỉ slide của prefix (giữ carousel/alpha-postX-1.png).

[2026-09-21 10:10:46 UTC] manual-verify (architect pass follow-up, user-requested) - success - Nghiệm thu refactor bằng cách chạy thật (không chỉ đọc code). Ba probe: (1) vòng đời carousel qua đúng handler create → list → claim → deliver → list → publish → drain → delete trên node:sqlite + R2 giả + Graph API giả (20 check); (2) src/admin/lib/videoQueue.js chạy trên React mini-runtime + fetch/timer giả (20 check) — Video KHÔNG đăng ký interval nào còn Carousel poll 20s và tự tắt khi hết job, cộng copy publish/delete; (3) mount thật Video.jsx + Carousel.jsx (esbuild của repo biên dịch JSX, antd/icon stub rồi đi cây element) để chứng minh mỗi trang chỉ hiện đúng kind của mình + từ vựng kind/status hai bên khớp (12 check). Lần chạy tìm ra 2 lỗi thật: (a) facebook.js kiểm tra cfg.asVideo TRƯỚC nhánh carousel, nên kênh Facebook bật as_video làm nút "Đăng FB" của carousel hỏng — video_key `carousel/<slug>` bị đưa sang publishFacebookVideo và IMAGES.get() trả null; đảo thứ tự (carousel trước as_video) vì prefix carousel không bao giờ là MP4. (b) social_queue.listSocialPosts join blog_posts bằng blog_post_id thô nên job carousel hiện post_title = null (tab Bài đăng mạng xã hội + thẻ ở Tổng quan hiển thị "—"); giờ dùng postIdFromRefSql như loadJobContext. Bằng chứng: 52 check probe pass, npm test 154 pass, build:functions compiled, admin:build OK.

[2026-09-21 10:19:06 UTC] manual-test (loop follow-up, user-requested) - success - Đưa các ca của pass verify vào harness của repo thay vì để probe ở /tmp. `scripts/run-platform-tests.js` thêm mục Q (R2 giả + Graph giả): carousel trên kênh Facebook bật as_video phải upload đủ photo và 0 lần gọi /videos, listSocialPosts trả đúng post_title/post_slug cho job carousel, delete chỉ xoá slide của slug mình và giữ carousel/alpha-postX-1.png cùng video/other.mp4. Frontend: script mới `scripts/run-admin-hook-tests.mjs` (+ `scripts/admin-hook-harness.mjs`, runtime React hook tối thiểu, map cả react lẫn antd) chạy thật src/admin/lib/videoQueue.js với fetch/timer giả — Video không đăng ký timer và không tự fetch lại, Carousel poll 20s rồi tự tắt khi hết job, publish/delete hiện đúng copy; kèm khẳng định call site trong Video.jsx/Carousel.jsx. npm test giờ chạy 3 suite: 9 + 157 + 8 check. Đã kiểm chứng test có răng bằng 4 đột biến (đảo lại thứ tự as_video, join thô sentinel, xoá slide theo prefix trần, bỏ qua cờ poll) — cả 4 đều fail đúng chỗ, sau đó khôi phục. Không phải sửa production code. `functions_dist/index.js` build ra chỉ khác tên tạm ngẫu nhiên của wrangler nên đã revert, không commit nhiễu.

[2026-09-21 10:36:00 UTC] manual-test (user-requested) - success - Smoke test cho đường render carousel của agent, không cần Chrome/hyperframes/network. Phần 1 (hợp đồng deck): 5 slide tài liệu độc lập 1080×1350, slide 1 = badge+hook, 2-4 = số thứ tự + đúng ý của nó, 5 = câu hỏi + URL đã bỏ scheme, script thiếu ý vẫn đủ 5 slide (pad), accent theo brand + fallback, text bài viết được escape, cùng input cho ra HTML y hệt (lý do snapshot ở --at nào cũng đúng), `slideQueries` riêng từng slide. Phần 2 (đường render thật): chạy `renderCarousel` với workspace tạm + `spawn`/`photo`/`deliver` giả — đúng 5 snapshot theo thứ tự deck, mỗi snapshot nhận index.html của slide mình và ảnh riêng của mình, payload deliver đúng job_id + 5 slide đúng thứ tự (tức đúng `carousel/<slug>-1..5.png` mà platform đặt tên), GuRouter tắt + host ảnh chết vẫn ra deck đủ, snapshot fail/no-PNG thì dừng trước khi deliver, deliver non-2xx hoặc 200-không-thành-công đều fail job. Seam injection tối thiểu: `renderCarousel(job, deps)` (mặc định = bản thật) + `assetsDir` cho `composeCarouselSlideHtml` + batch loop gate theo entry point; đã kiểm lại gọi trực tiếp vẫn claim job như cũ, thiếu config vẫn fail như cũ. 4 đột biến (bỏ pad, bỏ escape, lệch index slide, cắt slide trong payload) đều bị bắt. npm test giờ 4 suite: 9 + 157 + 8 + 14 check.
[2026-09-21 13:13:56 UTC] manual-fix (ad-hoc, user-requested) - success - Video post không có nhạc nền. Lần đầu chạy render THẬT (npx hyperframes@0.8.56 render -q draft, Chrome headless + ffmpeg có sẵn), ba biến thể của đúng composition mà renderOne tạo ra (voice + bed, voice đơn, bed đơn) rồi đo bằng ffmpeg ebur128/volumedetect: bed cũ = -60.6 LUFS trong mix, tức 31.2 LU dưới giọng đọc (-29.4 LUFS) — file bed chỉ đạt -33.3 dBFS peak và nội dung 400-2000 Hz ở -42.2 dB, nên video nghe/đo y như không có nhạc. Nguyên nhân nằm trong makeBgm: `amix` chia cho số input (mất 9.5 dB), biên độ sine quá thấp, và hợp âm 220-330 Hz nằm dưới ngưỡng loa điện thoại. Sửa: `amix=inputs=3:normalize=0` + sine 0.30/0.34/0.30 + hợp âm lên một quãng tám (440/554.4/659.2 v.v.) + lowpass 2400; `out` injectable để test ghi ra tmp; thêm log khi không tạo được bed (trước đây im lặng). Render lại sau sửa: bed -46.7 LUFS trong mix (17.3 LU dưới giọng), band 400-2000 Hz -39.4 dB. Test: 3 check mới trong scripts/run-video-agent-tests.mjs (level bed, band loa điện thoại, độ dài, và track nhạc trong composition đúng gain) — 17 check, cả 2 đột biến (bỏ normalize=0; đổi data-volume) đều bị bắt. npm test 4 suite (9 + 157 + 8 + 17), build:functions compiled, functions_dist revert (nhiễu token).

[2026-09-21 14:24:25 UTC] manual-fix (ad-hoc, user-requested) - success - Mix cuối của video post chưa được master: render THẬT đo -29.4 LUFS (TP -20.3 dBFS, LRA 0.7), tức ~15 LU dưới chuẩn mạng xã hội nên phát lại rất nhỏ. Thêm `masterLoudness()` vào video-agent/render-video.mjs: đo file MP4 đã mix bằng ebur128 (I + true peak) rồi scale TOÀN BỘ mix bằng MỘT gain tĩnh về -14 LUFS, chặn để true peak không vượt -1.5 dBTP; video stream copy, audio re-encode AAC 192k; mix không đo được (không có audio stream, thiếu ffmpeg) thì trả null và renderOne deliver nguyên bản kèm log. Chọn gain tĩnh thay vì `loudnorm=...:linear=true` vì loudnorm chỉ chịu linear khi LRA đo được ≠ 0 VÀ gain không vượt trần TP, còn lại tự chuyển sang dynamic mà không báo — gain biến thiên theo thời gian sẽ nâng bed ở mọi khoảng lặng của giọng, tức đổi cân bằng nhạc/giọng. Không đụng `data-volume="0.12"` hay makeBgm (pass trước). Nghiệm thu bằng render THẬT mới (npx -y hyperframes@0.8.56 render, Chrome + ffmpeg có sẵn, 9.6s / 680KB): -29.4 → -14.1 LUFS, TP -5.0 dBFS, LRA giữ nguyên 0.7, duration + stream y hệt (h264+aac), gain đúng MỘT giá trị +15.37 dB (spread 0.19 dB qua 24 cửa sổ, residual -32 dB = nhiễu AAC) — mix chỉ bị nhân một hằng số nên tỉ lệ giọng/nhạc không thể đổi. Test: 4 check mới trong scripts/run-video-agent-tests.mjs (mix kể chuyện có khoảng lặng + bed thật ở gain 0.12 phải đạt -14±1 LUFS và ≤ -1.5 dBTP khi ĐO LẠI bằng ebur128 độc lập; gain phải là một hằng số, spread ≤ 0.3 dB; mix hết headroom thì bị chặn ở trần chứ không clip; mix không đo được thì trả null) — 21 check. 4 đột biến đều bị bắt: đổi sang loudnorm dynamic (spread 1.14 dB), gain = 0, gain +12 dB, bỏ trần TP (TP -0.8 dB). npm test 4 suite (9 + 157 + 8 + 21), node --check OK. Chưa commit.

[2026-09-21 14:46:09 UTC] manual-verify (renderOne seam, user-requested) - success - Pass trước chỉ test `masterLoudness`; `renderOne` (nơi quyết định file nào lên platform) không có seam nào nên vị trí bước master không chứng minh được. Thêm seam tối thiểu `renderOne(job, deps)` với `work`/`spawn`/`deliver` (như renderCarousel): mọi path trong renderOne đi qua `work`, mọi subprocess qua `spawn`, deliver qua `deliver`, nhánh carousel forward deps; `makeBgm`/`masterLoudness` nay nhận out path tường minh trong `work` (trước đây dựa vào default = WORK của module, nên workspace tạm sẽ ghi vào repo). Test mới trong scripts/run-video-agent-tests.mjs (GuRouter được stub + fetch giả chặn mọi request khác): (1) happy path — chỉ hyperframes bị giả, còn mọi ffmpeg/ffprobe là thật (bed, độ dài TTS, master); file render giả được đặt đúng mức production (-29 LUFS) nên nhận được **bytes gửi lên deliver y hệt `renders/master.mp4`** (152317 B), loudness **-29 → -14.0 LUFS**, TP -9.5 dBTP, mtime master ≥ mtime bản thô; (2) hai nhánh hỏng — render không có audio và file ffmpeg không đọc được — đều **gửi bản thô kèm log `loudness master skipped`**, không có file master nào (không im lặng); (3) job carousel vẫn đi đúng nhánh carousel (5 snapshot, `/carousel-deliver`, không có thư mục `renders`). Kiểm thêm ngoài suite: chạy `renderOne` chỉ giả `spawn`/`deliver` (để `work` = default production) → ghi đúng vào `video-agent/workspace`, delivered = master, -29 → -14 LUFS, sau đó dọn sạch. Lần chạy thật lộ **lỗi trong chính harness mới**: `assert.deepEqual` trên hai Buffer ~100-160KB làm Node diff chúng khi fail, cả tiến trình bị OOM-kill (exit 137) thay vì báo lỗi — đã đổi sang `equals()` + so size. 4 đột biến đều bị bắt: deliver bản thô, bỏ log skip (im lặng), nhánh carousel rơi vào đường video, gỡ hẳn bước master. `npm test` 4 suite (9 + **162** + 8 + **24**), `node --check` OK. Chưa commit.

[2026-09-21 16:15:19 UTC] manual-fix (job thật trên VPS, user-requested) - success - Lần đầu chạy trọn một job video THẬT trên render VPS `node2-prod` (14.225.206.136, systemd `pages-seo-video.service` + timer 5 phút): enqueue qua `POST /api/admin/video/enqueue-missing` (project-scoped, limit 1) → timer claim job `giam-chi-phi-bao-bi-cho-doanh-nghiep-2026` → GuRouter script → edge-tts vi-VN (5.4+15.6+3.4+2.6s) → Chrome render (hyperframes 0.8.56) → master → deliver. RÀNG BUỘC: ffmpeg trên VPS là **4.4.2 (Ubuntu 22.04)**, không phải 8.x như máy dev — nó **từ chối** `ebur128=peak=true:framelog=quiet` (exit 1, "Error reinitializing filters!") nên không bao giờ in `Summary:` và `masterLoudness` trả null trên MỌI render thật (log `loudness master skipped`), dù 24 check local xanh vì ffmpeg 8.1.2 chấp nhận giá trị đó. Sửa: chỉ dùng `ebur128=peak=true`, và khi thiếu Summary thì log rõ exit code + 2 dòng stderr cuối trước khi trả null (bỏ im lặng). Ngoài ra VPS đang chạy code commit `7a3aa1e` (không có masterLoudness, bed cũ) nên đã deploy HEAD lên (backup `render-video.mjs.bak-7a3aa1e-20260921-230639`). Kết quả đo: file đã phát hành `video/giam-chi-phi-bao-bi-cho-doanh-nghiep-2026-1790007139262.mp4` = **-14.6 LUFS / TP -1.5 dBTP / LRA 3.2** (28.67s, h264+aac); bytes phát hành chứng minh bằng hash: R2 object = URL công khai `/image/...` = `master.mp4` trên VPS (sha256 `6203165817…`). Đối chiếu: raw của chính job đó trên VPS -17.1 LUFS (TP -4.0, LRA 3.2); bản do code cũ phát hành -16.8; bản phát hành khi master còn fail im lặng -16.6. ĐÍNH CHÍNH tiền đề của các pass trước: mix THẬT đo -16.6…-17.1 LUFS chứ không phải -29 (con số -29 đến từ render tổng hợp local có "giọng" là pink noise), nên master thật chỉ nâng ~2.5 dB và **trần true-peak chặn trước** (gain = min(2.6, 2.5) dB) → ra -14.6 chứ không phải -14.0; LRA và duration không đổi, video stream copy. Tác động: thay file agent trên VPS, tạo 2 job (cả hai `done`, attempts 1), KHÔNG có bài nào tự lên Facebook (các project này không bật `as_video`), timer/service để nguyên. `npm test` 4 suite (9 + 162 + 8 + 24), `node --check` OK, `functions_dist` sạch.

[2026-09-21 16:36:59 UTC] manual-jev-cost (docs typesafe + đo thật, user-requested) - success - Đọc `/concepts/state`, `/concepts/confidence`, `/models`, cookbook `parallel_questions`. RÀNG BUỘC GIÁ: **chỉ tính input token** ($0.042/Mtok), output miễn phí, chỉ có MỘT model (`jev-1.13.0` / alias `jev-latest`) nên không có tier rẻ hơn — mọi đòn giảm chi phí đều là "gửi ít hơn hoặc không gọi". Đo bằng API thật: khối 9 câu hỏi (8 dimension + overall, mỗi câu 3 mức criteria) tốn **~0.7-1k token mọi call** (file 613 ký tự được 975 token ⇒ rubric là phần lớn hoá đơn); một diff thật của autofix ~4.5k token (state ~80%). Sửa `scripts/jev.js`: `evaluate` gộp N tài liệu vào **một** call (state = `documents[]` named fields, khoá câu hỏi `d<i>_`) — đo trên 4 file thật (613-9804 ký tự): 4 call → 1 call, 7271 → 7289 token (**token trung tính**), **4.67s → 1.40s (3.3×)**; con số 12.2× của cookbook là khi CÙNG một state bị hỏi nhiều call, không áp cho các tài liệu rời (đã ghi rõ trong JEV.md để không kỳ vọng sai); trần 32k state+longest question và accuracy giảm khi state lớn nên giữ batch nhỏ. `--summary` nay thêm `in=<tokens>` vào cuối mỗi dòng ⇒ chi phí từng gate nằm ngay trong log này. Không mua phán quyết khi không có gì để chấm: `evaluate` không có tài liệu thì exit 2 **không gọi**, và runner bỏ gate khi branch không đổi file (row ghi `nothing to judge`) — bằng shim đếm được 0 call ở ca đó. LỖI THẬT lộ ra khi chạy: runner lấy diff **trước** bước commit nên `git diff` rỗng nếu implementer tự commit trong worktree → cả verifier, denylist lẫn Jev đều nhìn diff rỗng trong khi branch thật sự có thay đổi (đã dựng lại được bằng mutation: verifier nhận diff rỗng, `main...HEAD` vẫn có file). Sửa: commit trước, `DIFF_FILE = git diff main...HEAD` (đúng bytes sẽ merge) và denylist soi cùng diff đó; nhờ vậy guard "rỗng thì không gọi" mới đúng. Harness 24 check (W1 uncommitted / W2 committed / W3 no change / MUT old ordering / T1 triage / E1 no documents) xanh hết, 3 lỗi của chính harness đã sửa (đếm dòng thay vì đếm call, `git branch` thêm tiền tố `+`, harness tự xoá chính nó). Dọn luôn 1 row rác trong STATE.md/loop-run-log.md từ pass trước (`2026-09-21 16:31:35 UTC ... jev: forced: proceed` — do stub sinh ra, không phải lần chạy thật, branch/worktree đã không còn). `npm test` 4 suite (9 + 162 + 8 + 24), `node --check` + `bash -n` OK. Chưa commit.

[2026-09-21 16:39:27 UTC] manual-wire-jev (user-requested) - success - Ghi lại pass nối Jev vào runner (phần thay đổi đứng trước row `manual-jev-cost` ở trên; cả hai cùng vào một commit). `scripts/loop-run.sh`: `jev_call` là chủ duy nhất của "chạy jev, lấy câu trả lời + gate" (`JEV_NOTE`/`JEV_RC`, stdout tách khỏi stderr vì lý do thất bại ghi ra stderr nên gộp vào sẽ đọc như một phán quyết; dùng `awk` thay `grep | tail` vì `pipefail` + set -e làm runner chết im khi grep không khớp). Autofix: sau verifier APPROVE + tests/build, Jev đọc chính diff và **chỉ `proceed` mới merge**; `review`/`stop` → row `escalated` (giữ branch/worktree, in diff, exit 1); Jev **unavailable** (thiếu key, mạng, API lỗi) **không chặn** nhưng ghi rõ lý do — verifier + tests + denylist phía trên vẫn còn. Cả hai nhánh triage (`issue-triage`, `triage`) nay gọi Jev chấm Top 5 bằng rubric `skills/issue-triage/jev-questions.json` **và ghi row** vào loop-run-log.md + bảng STATE.md (trước đây hai nhánh này không log gì). `scripts/jev.js`: `--summary` in một dòng đầy đủ score + confidence (**trước** dòng JSON nên parser cũ không vỡ), dấu `!` cho câu không vượt ngưỡng; lỗi API in `error=<message>` đọc được thay vì dump JSON thô (và mode một-câu-hỏi cũng vậy). Docs: `JEV.md` sửa câu SAI trước đây ("exit code là quyết định" chỉ đúng với `evaluate`; `decide/verdict/score/ask` luôn exit 0 nếu API trả lời, phải đọc `gate.<key>.act`) và ghi rõ `--file` âm thầm bỏ qua file không có đuôi (dùng `--state -`); `loop-constraints.md` thêm 2 bullet mô tả đúng cách runner dùng Jev. Chạy thật (Jev thật, agent/npm stub bằng PATH shim): triage có item → `type=noise@0.67 area=infra@0.72 priority=P1(2.01)@0.99 …`; Top 5 rỗng → `nothing to score`; autofix + Jev thật trên diff thật → `review` → escalated, branch giữ, exit 1; gate ép proceed → row dry-run exit 0; gate ép review → escalated + in diff + exit 1; thiếu key và key sai (401) → `unavailable — … error=Cannot authenticate…` → **chạy tiếp**; merge thật chạy trong clone ở /tmp (không đụng main) → ff-merge, xoá worktree+branch, row `merged … jev: proceed overall=proceed@0.91`. Lỗi do chính phần này gây ra, phát hiện khi chạy: pipe làm mất `JEV_NOTE` (subshell) → truyền state bằng argument; diff qua `--file` bị `DOC_EXT` bỏ → "no documents" (exit 2 = stop cho MỌI autofix) → đưa diff qua stdin; `grep \| tail` dưới pipefail giết runner → `awk`; trộn stdout/stderr → tách; dòng placeholder `- (empty …)` trong Top 5 lọt qua guard → sửa. `bash -n` + `node --check` OK, `npm test` 4 suite (9 + 162 + 8 + 24), `build:functions` compiled (nhiễu `functions_dist` đã revert).

